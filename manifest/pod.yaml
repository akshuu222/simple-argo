# apiVersion: v1
# kind: Pod
# metadata:
#   name: nginx-pod
#   labels:
#     app.kubernetes.io/name: nginx-pod
# spec:
#   containers:
#   - name: nginx-pod
#     image: nginx:alpine
#     ports:
#       - containerPort: 80

# apiVersion: v1
# kind: Pod
# metadata:
#   name: nginx-pod
#   labels:
#     app.kubernetes.io/name: nginx-pod
#   metadata:
#   annotations:
#     argocd.argoproj.io/sync-options: Prune=confirm
# spec:
#   containers:
#   - name: nginx-pod
#     image: nginx:alpine
#     ports:
#       - containerPort: 80
